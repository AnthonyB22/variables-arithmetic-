let  salaryPerYear
let daysPerYear
let  salaryPerDay

salaryPerYear = (Get)
daysPerYear = (Get)
// if daysPerYear is 0, then divide-by-zero causes program termination. salaryPerDay = salaryPerYear / daysPerYear
console, log  ("Salary per day is:" )
console , log  (salaryPerDay)